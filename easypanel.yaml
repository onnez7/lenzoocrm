name: lenzoocrm
type: nodejs
build:
  method: native
  command: npm run install:all && npm run deploy:build
start:
  command: npm run start
  port: 3001
environment:
  NODE_ENV: production
  PORT: 3001 