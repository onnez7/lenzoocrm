[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = [
  "npm ci",
  "cd backend && npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "cd backend && npm run build"
]

[start]
cmd = "cd backend && npm start" 