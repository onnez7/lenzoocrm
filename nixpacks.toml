[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = [
  "chmod +x build.sh",
  "npm install",
  "cd backend && npm install"
]

[phases.build]
cmds = ["./build.sh"]

[start]
cmd = "cd backend && npm start" 